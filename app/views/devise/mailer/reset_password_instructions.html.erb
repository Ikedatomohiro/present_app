<p>こんにちは、 <%= @resource.email %>　さん!</p>
<%= @resource.password %>
<p>以下のリンクからパスワードを変更することができます。</p>

<p><%= link_to 'このリンクにアクセエスしてパスワードを発行する。', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールに心当たりのない方はメールを破棄して下さい。</p>
<p>上のリンクにアクセスするまでパスワードは再発行されません。</p>
